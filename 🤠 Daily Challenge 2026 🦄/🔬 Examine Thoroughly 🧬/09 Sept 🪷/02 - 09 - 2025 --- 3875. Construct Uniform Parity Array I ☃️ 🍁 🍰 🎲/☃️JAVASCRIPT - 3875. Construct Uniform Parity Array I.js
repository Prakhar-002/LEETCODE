//! https://github.com/Prakhar-002/LEETCODE

// Todo 💎 QUESTION NUMBER 3875

//? ⌚ Time complexity ➺ O(1) 👉🏻  n = len(nums)

//? 🧺 Space complexity ➺ O(1)

var uniformArray = function(nums1) {
      // We can always form an all-odd array (if mixed) or keep the original array (if uniform):
      // Even - Odd = Odd, so every even number can be converted to an odd number
      // using an odd element from nums1.
      return true;
};